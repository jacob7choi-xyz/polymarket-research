# Docker ignore file - exclude from build context
# Interview Point: Smaller build context = faster builds

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Virtual environments
venv/
env/
.venv

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Git
.git/
.gitignore

# Documentation
*.md
# ...except README.md, which pyproject.toml declares as `readme`. The build backend opens
# it while building the project wheel, so excluding it fails `uv sync` with
# "failed to open file /app/README.md". Not documentation from the build's point of view.
!README.md
docs/

# Logs
logs/
*.log

# Environment
.env
.env.local

# Docker
Dockerfile
docker-compose.yml
.dockerignore

# CI/CD
.github/
.gitlab-ci.yml

# Monitoring (already in image)
monitoring/

# OS
.DS_Store
Thumbs.db
